import React from 'react';
import { Box, Typography } from '@mui/material';

const Alerts: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Alerts
      </Typography>
      <Typography>Alert management and notifications</Typography>
    </Box>
  );
};

export default Alerts;
